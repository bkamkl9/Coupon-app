name: Deploy app to github pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 21

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Install turbo repo
        run: npm install -g turbo

      - name: Install dependencies
        run: cd frontend && pnpm install

      - name: Build the app
        run: cd frontend && turbo build
        env:
          NODE_ENV: production
          VITE_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5jcHNkc2pkc25ncG9kemRzbHNpIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTg4Nzc1MDEsImV4cCI6MjA3NDQ1MzUwMX0.F6wfkfLba0imxZ4cx8JMPeK7alu5z8hOCE7snobpHog
          VITE_SUPABASE_URL: https://ncpsdsjdsngpodzdslsi.supabase.co

      - name: Copy admin dist to output directory
        run: |
          mkdir -p ./output/admin
          cp -r ./frontend/apps/admin/dist/* ./output/admin/

      - name: Copy admin dist to output directory
        run: |
          mkdir -p ./output/mobile
          cp -r ./frontend/apps/mobile/dist/* ./output/mobile/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./output
